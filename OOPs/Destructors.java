package OOPs;

public class Destructors {
    
    //Java does not have the concept of a destructor like in C++. 
    // Instead, Java uses automatic garbage collection to manage memory, 
    // which automatically reclaims memory from objects that are no longer referenced. 
}
